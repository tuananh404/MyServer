{
  "name": "imgui-license-dashboard",
  "version": "4.4.0",
  "description": "ServerKey web control plane for licenses, devices, sessions, remote policy, and feature flags.",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js",
    "dev": "node api/index.js",
    "check": "node --check api/index.js && node --check public/app.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
